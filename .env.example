# データベース設定
# PostgreSQL（本番環境・Render.com）
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# 開発環境設定
NODE_ENV=development

# 本番環境設定（Render.comで自動設定）
# NODE_ENV=production
# DATABASE_URL=（Render.comが自動で設定）